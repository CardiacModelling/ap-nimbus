# `ApPredict` without Lookup Tables (aka emulators) pre-installed

`ApPredict` but with Lookup Tables downloaded on-demand if required.

Documentation is available at https://ap-nimbus.readthedocs.io/.